# glmnet varImp handles multi-response fits and a missing lambda

    Code
      code$varImp(structure(list(), class = "fake_glmnet"))
    Condition
      Error in `code$varImp()`:
      ! must supply a value of lambda

