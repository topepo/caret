# featurePlot handles a single predictor

    Code
      featurePlot(one, iris$Species, "pairs")
    Condition
      Warning in `seq_len()`:
      first element used of 'length.out' argument
      Error in `seq_len()`:
      ! argument must be coercible to non-negative integer

